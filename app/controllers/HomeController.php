<?php

namespace app\controllers;

class HomeController
{
    public function index()
    {
        // $user = new User;
        // $user = $user->find('id', 5);

        // view('app/views/home.php', ['user' => $user]);
        var_dump('index na home');
    }
}
